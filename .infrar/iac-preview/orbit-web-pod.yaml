# Generated by Infrar — committed on purpose, and run exactly as it is.
#   generated by:  infrar-import
#   generated at:  2026-09-08T15:53:16.799Z
#   rule version:  2
#
# This file carries the BEHAVIOUR of orbit-web. Its IDENTITY — the object's name,
# its reference labels and its pod key — is derived from the commit that contains this
# file, so it cannot be written here; Infrar puts it on when the Preview is composed.
#
# Two more things are missing on purpose, and for the same reason — they are facts of one
# Preview, not of this commit. The IMAGE Infrar builds from your code (it is pinned to the
# digest that build produced), and the VALUES of the variables (per-Preview addresses and
# secrets, which do not belong in a repository). The NAMES are here; the values arrive at
# compose. Images you declare yourself — a sidecar, a tool — stay exactly as you write them.
#
# THE RESOURCES ARE NOT HERE, AND THEY ARE OPTIONAL. The cpu, memory and ephemeral-storage a
# pod is granted come from the plan of the organization running it, shared over everything
# that Preview stands up: a fact of one launch, not of this commit. A number written here on
# the day the file was born would go on being used after the plan changed, and the pod would
# run undersized in silence. Say nothing and every launch grants the CURRENT figures. Declare
# them and they are honoured as a ceiling to grant up to — what the plan can give, never
# more, and never a reason to refuse a launch.
#
# Edit it freely: anything Kubernetes accepts is accepted here too. A handful of fields
# are refused (they would break the isolation of the preview plane, or decide something
# only the platform can decide) — a launch tells you which, and offers a corrected file.
apiVersion: v1
kind: Pod
metadata:
  annotations:
    infrar.io/generated-by: infrar-import
    infrar.io/generated-at: 2026-09-08T15:53:16.799Z
    infrar.io/iac-rules-version: "2"
spec:
  containers:
    - name: app
      command:
        - /bin/sh
        - -c
        - node server.js
      ports:
        - containerPort: 3000
      readinessProbe:
        httpGet:
          path: /
          port: 3000
        initialDelaySeconds: 3
        periodSeconds: 5
